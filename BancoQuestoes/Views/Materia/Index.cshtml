@model IEnumerable<BancoQuestoes.Models.Materia>

@{
    ViewData["Title"] = "Matérias";
}

<div class="crud-page">

    <div class="crud-header">

        <h1 class="crud-title">
            Matérias
        </h1>

        <a asp-action="Create"
           class="crud-btn">
            + Nova Matéria
        </a>

    </div>

    <div class="crud-card">

        <table class="crud-table">

            <thead>
                <tr>
                    <th>Período</th>
                    <th>Nome da Matéria</th>
                    <th style="width:220px;">
                        Ações
                    </th>
                </tr>
            </thead>

            <tbody>

            @foreach (var item in Model)
            {
                <tr>

                    <td>

                        <span class="crud-badge">
                            @item.Periodo
                        </span>

                    </td>

                    <td>
                        @item.Nome
                    </td>

                    <td>

                        <div class="crud-actions">

                            <a asp-action="Edit"
                               asp-route-id="@item.MateriaId">
                                Editar
                            </a>

                            <a asp-action="Details"
                               asp-route-id="@item.MateriaId">
                                Detalhes
                            </a>

                            <a asp-action="Delete"
                               asp-route-id="@item.MateriaId">
                                Excluir
                            </a>

                        </div>

                    </td>

                </tr>
            }

            </tbody>

        </table>

    </div>

</div>