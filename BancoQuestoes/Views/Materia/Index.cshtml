@model IEnumerable<BancoQuestoes.Models.Materia>

@{
    ViewData["Title"] = "Index";
}

<h1>Matérias</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Periodo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Periodo)
            </td>
            <td>
                @Html.DisplayFor(model => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MateriaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MateriaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MateriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
